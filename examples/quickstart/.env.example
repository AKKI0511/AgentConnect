# Used only by `uv run python -m team_demo --live`.
# Deterministic mode needs none of these.

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional. Point at any OpenAI-compatible endpoint.
# OPENAI_BASE_URL=https://api.openai.com/v1
